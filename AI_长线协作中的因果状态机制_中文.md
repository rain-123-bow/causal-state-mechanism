# AI 长线协作中的因果状态机制

## 简述

当前，Google、Anthropic、LangChain 等主流路线都已经把关注点从单纯的 prompt 设计，转向 context engineering、长会话状态管理和跨会话 memory。核心目标不再是把历史对话持续堆进上下文，而是通过结构化存储、相关性筛选、压缩和按需回注，降低长链路任务中的成本、延迟和失稳风险。Google 甚至明确把 context 描述成对更大状态系统的一次 “compiled view”，Anthropic 也把 context engineering 直接定义为长期 agent 构建中的关键问题，LangGraph 则已经把跨会话长期记忆、线程状态和可持久化 agent state 做成正式能力。

主流实践目前更多在解决“什么信息该被保留、什么时候被取回、如何控制上下文成本”；本文更关心的是“这些信息在进入后续推理后，应当如何按因果关系被组织、按优先级被路由，以及这种展开顺序是否应当因使用者的注意力习惯和证据容忍度而变化”。

也就是说，这里讨论的并不只是 memory system，而是一个面向长线协作的 external reasoning protocol：它不是简单缓存结论，而是维护一个带有因果、证据、作用域和注意力建议的结构化状态。

## 原方法及局限性

最初采取的方法本质上是一种“结论缓存”机制：在一次推理或实验结束后，不保留完整推理过程，只保留最终结论，并在后续会话中直接复用这些结论，以避免同一条推理链被重复展开。有时候为了跨会话同步，会额外保留其他核心语义。

这里的目标很直接：用结论代替重复推理，从而降低长会话中的额外推理成本。

这一方法在轻量任务和短链路会话中通常是成立的，因为在这类任务中，结论数量有限，结论之间的依赖关系较弱，问题链路较短，而生成这些结论的原因、条件和证据往往仍停留在近期上下文中。这种场景下，“结论”可以近似替代其生成过程，后续任务更多依赖的是“已经得到什么判断”，而不是重新审视“这个判断为什么成立”。

但是这种“近似”在长线任务中会逐渐失效，尤其是在实验密集型任务中。因为结论并不是孤立的信息节点，而是某个条件集合、证据集合和因果链条压缩后的结果。只保存结论，实际上等于丢弃了结论成立所依赖的前提、作用域、证据强度以及与其他结论之间的依赖和覆盖关系。在短会话中，这些信息仍可由局部上下文隐式补足；但在跨阶段、跨会话、跨实验轮次的协作中，这种隐式补足会迅速失效。

而且在单会话的长线任务中，上下文窗口是有限的，每一次上下文压缩都会带来一定的语义信息损失，原本精确的语义随着压缩次数增加，可能会变得不那么精确，尤其是推理的隐含条件。后续的推理依赖的可能不只是先前得到的结论，也可能需要先前结论成立的原因（尤其是实验类、测试类任务），因为后续的结论很有可能推翻前置结论，此时如果没有前置结论成立的“因”，很可能无法准确得到下一步结论。因此，“因果链”或更准确地说“因果结构”的保留，在长线任务中几乎是必须的。

此外，有时候为了响应速度或者规避推理负担过重的问题，会切换到新的会话，然后进行状态同步。此时仅保留因果结构也是不够的。因为因果结构所在的文件中，能被保留下来的几乎都是“被认为是重要的”。如果只是把整个文件给到新的会话，新的会话虽然上下文窗口变小了，但是注意力机制还在；当注意力机制判定整个文件都很重要的时候，推理负担实际上并没有减小多少。因此，除了保留因果之外，也需要实现一种“外部注意力机制”。

因此，“只存结论”更适合作为轻会话中的局部优化，而不是长线协作中的稳定机制。它能够减少一部分重复推理，但不足以支撑跨阶段任务中的持续对齐。真正需要被维护的，不只是结论本身，而是结论背后的因果结构，以及这些因果结构在当前任务中的优先级和展开顺序。

## 新的方案

> 请注意，此方案仅为个人建议，并非成熟或通用方案。

### 因果结构

这里的“因果”不是抽象哲学概念，而是一个结论成立所需的最小支持结构。也就是：一个结论为什么成立、在什么条件下成立、由什么证据支持、依赖哪些前提、会影响哪些后续判断。

这里的“因果”至少包含六类信息：

- **结论本身**：得到了什么判断
- **原因**：为什么会得到这个判断
- **证据**：代码（可通过指定文件的范围来代替存储源代码）、日志、实验结果、统计数据、现象
- **作用域**：这个结论在什么范围内有效
- **前提 / 假设**：它依赖哪些条件
- **关系**：依赖谁、覆盖谁、推翻谁

“因果”不是完整推理过程的原样记录，而是结论可被后续推理复用所需的最小逻辑结构。下面是一个最小示例：

```text
Fact:
  id
  statement
  why
  evidence
  scope
  assumptions
  depends_on
  supersedes / invalidates
  confidence
```

- `statement`：结论本身
- `why`：最小原因，不展开完整过程
- `evidence`：结论依据
- `scope`：适用范围
- `assumptions`：依赖前提
- `depends_on`：依赖哪些旧结论
- `supersedes / invalidates`：覆盖或推翻哪些旧结论
- `confidence`：当前置信度

因果结构的目标，不是复现一次完整思考，而是保证未来在条件变化、问题切换或结论冲突时，系统仍能判断“这个结论还能不能继续使用”。

### 外部注意力机制

这里的“外部注意力机制”不是试图替代模型内部的注意力机制，而是在模型推理之前，先对外部状态进行一次结构化路由：决定当前问题下哪些信息应被优先关注，哪些只作为背景保留，哪些暂时不展开。

这个机制解决的问题：

- 当前问题最相关的是哪些结论
- 哪些因果节点必须展开
- 哪些节点可以只保留摘要
- 哪些旧结论虽然重要，但当前不相关
- 哪些内容已经过时或被覆盖

它本质上在做三件事情：**选择**（当前问题要读哪些信息）、**排序**（哪些部分信息先进入推理）、**展开控制**（哪些只看结论，哪些必须展开到原因和证据）。

外部注意力机制在会话中工作时，不是由模型为每个因果结构长期固定一个“权重”，而是由外部系统根据当前问题、已有因果结构和策略配置，动态计算本轮推理所需的“优先级分数”或“路由分数”。这个分数不是模型内部注意力的直接外显，而是一种外部路由结果，用来决定哪些信息应优先进入当前推理，哪些只保留摘要，哪些需要展开到原因和证据层，哪些可以暂不展开。模型可以参与部分语义判断，例如相关性、展开需求和潜在冲突，但最终分数应由外部机制统一计算；必要时，再由使用者或维护者对策略配置进行校正。

请注意，这个机制中有部分依赖个人风格。

**不依赖个人风格的部分（相对通用）：**

- 是否与当前问题直接相关
- 是否为高置信硬事实
- 是否已经被更新证据推翻
- 是否属于当前任务的全局约束
- 是否是后续推理的关键依赖

**明显依赖个人风格的部分（受使用者习惯影响）：**

- 是先看结论还是先看推导
- 对经验性假设的容忍度
- 对证据完整度的要求
- 对低置信结论是否允许先纳入推理
- 更偏保守还是更偏探索
- 更偏局部最优还是全局完整

外部注意力机制的骨架可以通用，但其策略层会受到使用者注意力习惯、证据门槛和推理偏好的影响，因此更适合设计成“通用协议 + 个性化策略”的形式，而不是唯一固定规则。简单的示例如下：

```text
Policy Profile:
  read_order: conclusion_first / causal_first
  evidence_threshold: low / medium / high
  allow_empirical_assumption: true / false
  expansion_rule:
    - high_relevance + low_confidence -> expand why + evidence
    - low_relevance + high_confidence -> keep statement only
```

在实现上，外部注意力机制并不一定需要一套精确的连续量化模型。对于当前阶段，更可行的方式是采用离散分级机制。因为这里的目标不是获得一个看似精确的数值，而是为当前问题提供可解释的路由与展开决策。相比连续分数，A/B/C/D/E/F 这类分级更容易定义标准，也更容易由 AI 辅助完成初步判断，再由使用者进行校正。

为了避免“一个等级同时承担‘是否进入推理’和‘是否展开到什么层级’”这两个问题，分级至少应拆成两类：路由等级，用于决定一条因果结构是否优先进入当前推理；展开等级，用于决定其只展示结论，还是继续展开到原因和证据层。这样可以在不引入复杂外部量化开发成本的前提下，先实现一个可运行、可解释、可校正的外部注意力机制。

如果想进一步追求更准确、稳定的表现，可以考虑引入更细致的分值计算机制；但在当前阶段，采用“AI 按规则分级 + 人工校正”的方式，往往已经足以支撑高强度的长线协作。

## 最后

以上内容并不是一个已经成熟的标准答案，而是我当前对长线 AI 协作机制的一次阶段性整理。写这份文档的目的，除了分享这部分思考，更想看看是否已经有人做过类似尝试，或者在相关方向上踩过类似的坑。若有现成实践、失败经验或不同判断，欢迎交流。
